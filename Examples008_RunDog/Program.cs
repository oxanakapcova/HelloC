int counte = 0;
int distance = 10000;
int firstFriendSpeed = 1;
int secondFriendSpeed = 2;
int dogSpeed = 5;
int firstFriendSpeed = 2;
int time = 0;

if distance <= 10
{
    Console.Write(count);
}
else
{
    if friend = 1 
    time = distance/(firstFriendSpeed + secondFriendSpeed);
}
else
{
    time = distance/(secondFriendSpeed + dogSpeed);
}
distance = distance(firstFriendSpeed + secondFriendSpeed) * time;

count++;

{
    Console.Write(counte);
}


